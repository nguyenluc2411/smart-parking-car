apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus            # uid cố định để các dashboard provisioned tham chiếu ổn định
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
